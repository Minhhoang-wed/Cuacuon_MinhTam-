import { FolderPlus, Save, Trash2 } from "lucide-react";
import { deleteCategory, saveCategory } from "@/lib/admin-actions";
import { getAdminCategories } from "@/lib/admin-data";
import { isSupabaseConfigured } from "@/lib/supabase-rest";

export default async function AdminCategoriesPage({ searchParams }: { searchParams: Promise<{ saved?: string }> }) { const categories = await getAdminCategories(); const demo = !isSupabaseConfigured(); const saved = (await searchParams).saved; return <>{saved && <div className="admin-success">Đã lưu danh mục.</div>}<header className="admin-page-header"><div><span>Catalog</span><h1>Danh mục</h1><p>Sắp xếp và tổ chức sản phẩm theo nhóm dễ tìm.</p></div></header><div className="admin-two-column"><section className="admin-panel"><div className="admin-panel-heading"><div><span>Danh mục hiện có</span><h2>{categories.length} danh mục</h2></div></div><div className="category-admin-list">{categories.map((category) => <article key={category.id}><form action={saveCategory} className="category-edit-form"><input type="hidden" name="id" value={category.id} /><div className="admin-fields two"><label><span>Tên</span><input name="name" defaultValue={category.name} required /></label><label><span>Slug</span><input name="slug" defaultValue={category.slug} /></label><label className="full"><span>Mô tả</span><textarea name="description" rows={2} defaultValue={category.description || ""} /></label><label><span>Thứ tự</span><input name="sort_order" type="number" defaultValue={category.sort_order} /></label><label className="check-field"><input name="is_active" type="checkbox" defaultChecked={category.is_active} /><span>Đang hiển thị</span></label></div><button className="button button-dark" disabled={demo}><Save /> Lưu</button></form><form action={deleteCategory} className="category-delete-form"><input type="hidden" name="id" value={category.id} /><button disabled={demo} title="Chỉ xóa được danh mục chưa có sản phẩm"><Trash2 /></button></form></article>)}</div></section><aside className="admin-form-card sticky-card"><FolderPlus /><h2>Thêm danh mục</h2><form action={saveCategory}><div className="admin-fields"><label><span>Tên danh mục *</span><input name="name" required /></label><label><span>Slug</span><input name="slug" placeholder="Tự tạo nếu bỏ trống" /></label><label><span>Mô tả</span><textarea name="description" rows={4} /></label><label><span>Thứ tự</span><input name="sort_order" type="number" defaultValue={categories.length + 1} /></label><label className="check-field"><input name="is_active" type="checkbox" defaultChecked /><span>Hiển thị ngay</span></label></div><button className="button button-primary" disabled={demo}>Thêm danh mục</button></form></aside></div></>; }
