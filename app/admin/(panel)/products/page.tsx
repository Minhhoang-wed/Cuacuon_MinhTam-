import { Archive, Edit3, Plus } from "lucide-react";
import Link from "next/link";
import { deleteProduct } from "@/lib/admin-actions";
import { getAdminProducts } from "@/lib/admin-data";
import { isSupabaseConfigured } from "@/lib/supabase-rest";

export default async function AdminProductsPage() { const products = await getAdminProducts(); const demo = !isSupabaseConfigured(); return <><header className="admin-page-header"><div><span>Catalog</span><h1>Sản phẩm</h1><p>Quản lý thông tin, giá hiển thị, trạng thái và SEO.</p></div><Link href="/admin/products/new" className="button button-primary"><Plus /> Thêm sản phẩm</Link></header><section className="admin-panel"><div className="admin-table-wrap"><table className="admin-table product-admin-table"><thead><tr><th>Tên sản phẩm</th><th>Danh mục</th><th>Giá</th><th>Trạng thái</th><th>Nổi bật</th><th aria-label="Thao tác" /></tr></thead><tbody>{products.map((product) => <tr key={product.id}><td><b>{product.name}</b><small>/{product.slug}</small></td><td>{product.category?.name}</td><td>{product.price_label || (product.price_amount ? new Intl.NumberFormat("vi-VN").format(product.price_amount) + "đ" : "—")}</td><td><span className={`status-badge ${product.status}`}>{product.status === "published" ? "Hiển thị" : product.status === "draft" ? "Bản nháp" : "Lưu trữ"}</span></td><td>{product.is_featured ? "Có" : "—"}</td><td><div className="table-actions"><Link href={`/admin/products/${product.id}`} aria-label={`Sửa ${product.name}`}><Edit3 /></Link><form action={deleteProduct}><input type="hidden" name="id" value={product.id} /><button disabled={demo} title="Lưu trữ sản phẩm"><Archive /></button></form></div></td></tr>)}</tbody></table></div></section></>; }
